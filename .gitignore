node_modules
.next
.env
.env.local
# Any other .env variant (.env.production, hand-made backups, ...). Only
# .env.example is tracked — it holds placeholders, never real credentials.
.env.*
!.env.example
*.log
*.tsbuildinfo

# Local dev-tool config (Claude Code launch config) — not part of the product
.claude/

# Local database backup output (scripts/backup.mjs)
backups/
